gcc compiler

[ gcc <input_filename>.c -o <output_filename> ]

getchar(); 要加才有辦法打印到cmd


q1: 打印7到負無限大的數值，每次間隔2
Output:Above program goes in infinite loop because n is never zero when loop condition (n != 0) is checked.

q2:  